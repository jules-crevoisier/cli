<template>
  <div class="admin-layout">
    <AdminSidebar />
    <main class="admin-content">
      <h1>Admin Dashboard</h1>

      <div v-if="pending" class="loading">Loading dashboard...</div>

      <div v-else class="stats-grid">
        <div class="stat-card">
          <h3>Total Users</h3>
          <p class="stat-value">{{ stats?.totalUsers ?? 0 }}</p>
        </div>
        <div class="stat-card">
          <h3>Total Items</h3>
          <p class="stat-value">{{ stats?.totalItems ?? 0 }}</p>
        </div>
        <div class="stat-card">
          <h3>New Users (7d)</h3>
          <p class="stat-value">{{ stats?.recentUsers ?? 0 }}</p>
        </div>
        <div class="stat-card">
          <h3>Recent Items (7d)</h3>
          <p class="stat-value">{{ stats?.recentItems ?? 0 }}</p>
        </div>
      </div>

      <section class="recent-section">
        <h2>Recent Activity</h2>
        <div v-if="stats?.latestItems?.length" class="recent-list">
          <div v-for="item in stats.latestItems" :key="item.id" class="recent-item">
            <span class="recent-title">{{ item.title }}</span>
            <span class="recent-date">{{ new Date(item.createdAt).toLocaleDateString() }}</span>
          </div>
        </div>
        <p v-else class="empty">No recent activity.</p>
      </section>
    </main>
  </div>
</template>

<script setup lang="ts">
definePageMeta({
  layout: 'default',
  middleware: ['auth', 'admin'],
})

interface DashboardStats {
  totalUsers: number
  totalItems: number
  recentUsers: number
  recentItems: number
  latestItems: Array<{ id: string; title: string; createdAt: string }>
}

const { data: stats, pending } = await useFetch<DashboardStats>('/api/admin/stats')
</script>

<style scoped>
.admin-layout {
  display: flex;
  min-height: 100vh;
}

.admin-content {
  flex: 1;
  padding: 2rem;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1.5rem;
  margin-bottom: 2rem;
}

.stat-card {
  background: white;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 1.5rem;
}

.stat-card h3 {
  font-size: 0.875rem;
  color: #718096;
  margin-bottom: 0.5rem;
  text-transform: uppercase;
}

.stat-value {
  font-size: 2rem;
  font-weight: 700;
  color: #1a202c;
}

.recent-section {
  margin-top: 2rem;
}

.recent-section h2 {
  margin-bottom: 1rem;
}

.recent-list {
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  overflow: hidden;
}

.recent-item {
  display: flex;
  justify-content: space-between;
  padding: 0.75rem 1rem;
  border-bottom: 1px solid #e2e8f0;
}

.recent-item:last-child {
  border-bottom: none;
}

.recent-title {
  font-weight: 500;
}

.recent-date {
  color: #718096;
  font-size: 0.875rem;
}

.loading,
.empty {
  text-align: center;
  padding: 2rem;
  color: #718096;
}
</style>
