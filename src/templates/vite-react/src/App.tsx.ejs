<% if (modules.includes('dark-mode')) { -%>
import { ThemeToggle } from "./components/ThemeToggle";
<% } -%>

function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
<% if (modules.includes('dark-mode')) { -%>
      <div className="absolute top-4 right-4">
        <ThemeToggle />
      </div>
<% } -%>
      <h1 className="text-4xl font-bold mb-4"><%= projectName %></h1>
      <p className="text-lg text-gray-600 dark:text-gray-400 mb-8">
        Get started by editing{" "}
        <code className="font-mono bg-gray-100 dark:bg-gray-800 px-2 py-1 rounded">src/App.tsx</code>
      </p>
<% if (modules.includes('auth') || modules.includes('admin')) { -%>
      <div className="flex gap-4 flex-wrap justify-center">
<% if (modules.includes('auth')) { -%>
        <a href="/login" className="rounded-lg bg-blue-600 px-4 py-2 text-white hover:bg-blue-700">Login</a>
        <a href="/register" className="rounded-lg border border-blue-600 px-4 py-2 text-blue-600 hover:bg-blue-50 dark:hover:bg-blue-950">Register</a>
<% } -%>
<% if (modules.includes('admin')) { -%>
        <a href="/admin" className="rounded-lg bg-gray-800 px-4 py-2 text-white hover:bg-gray-900 dark:bg-gray-700 dark:hover:bg-gray-600">Admin</a>
<% } -%>
      </div>
<% } -%>
    </main>
  );
}

export default App;
